{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "renovate-operator",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/src/cmd",
      "cwd": "${workspaceFolder}/src",
      "buildFlags": "-tags timetzdata",
      "envFile": "${workspaceFolder}/.env",
      "console": "integratedTerminal"
    }
  ]
}
